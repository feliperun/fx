# Upstream pull requests carrying fx-faberun patches: <number> <patch subject>.
# A merged PR's commit disappears from fx-faberun on the next sync, because
# the rebase finds the same change already upstream.
1043 Report prompt cache reads from Chat Completions providers
1045 Stop the skill walk at the repository when HOME is not above it
